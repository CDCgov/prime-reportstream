<script type=module>
  import api from '/js/prime_api.js'
  window.prime_rest_api = window.prime_api = api

  api.mock_login = async function (reload=true) {
    let {token} = await this.json_get('_mock_login_')
    await this.jwt_login(token)

    if (reload) location.reload()
  }
</script>
