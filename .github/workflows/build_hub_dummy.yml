name: Build Hub

on: push
  # pull_request:
  #   branches:
  #     - master
  #   paths-ignore:
  #     - "prime-router/**"
  # push:
  #   branches:
  #     - master
  #   paths-ignore:
  #     - "prime-router/**"

jobs:
  Build Router:
    name: Build Router
    runs-on: ubuntu-latest
    steps:
      - name: Dummy Build
        run: 'echo "No build required"'
  