name: Build Hub Dummy

on:
  pull_request:
    branches:
      - master
    paths-ignore:
      - "prime-router/**"
  push:
    branches:
      - master
    paths-ignore:
      - "prime-router/**"

jobs:
  dummy_job:
    name: Dummy job
    runs-on: ubuntu-latest
    steps:
      - name: Dummy Build
        run: echo "This is the build_hub dummy workflow "
  