name: Check Key Vault Certificates Expiration

on:
  push: 
    branches:
      - feature/check-keyvauly-cert-expiration
    
jobs:
  check-certificates:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Changes
        uses: actions/checkout@24cb9080177205b6e8c946b17badbe402adc938f

      - name: Get Runner IP
        run: echo "::set-output name=runner_ip::$(curl https://ifconfig.me)"
        id: runner_ip

      - name: Connect to VPN & Login into Azure
        uses: ./.github/actions/vpn-azure
        with:
          tls-key: ${{ secrets.TLS_KEY }}
          ca-cert: ${{ secrets.CA_CRT}}
          user-crt: ${{ secrets.USER_CRT }}
          user-key: ${{ secrets.USER_KEY }}
          sp-creds: ${{ secrets.SERVICE_PRINCIPAL_CREDS }}

      - name: Add Runner IP to Key Vault Firewall
        uses: azure/cli@v1
        with:
          inlineScript: |
            az keyvault network-rule add --name pdhstaging-keyvault --ip-address ${{ steps.runner_ip.outputs.runner_ip }}

      - name: Get Certificates Expiration
        run: |
          az keyvault certificate list --vault-name pdhstaging-keyvault --query "[].{name:name, expires:attributes.expires}" --output table

      - name: Get Near Expiry Certificates
        id: cert-expiration
        run: |
          near_expiry_date=$(date -d "+400 days" -I)
          near_expiry_certificates=$(az keyvault certificate list --vault-name pdhstaging-keyvault --query "[?attributes.expires<=\`$near_expiry_date\`].name" -o tsv)
          echo "The following certificates will expire within the next 400 days: $near_expiry_certificates" 
          echo "::set-output name=expiring_certs::$near_expiry_certificates" > near_expiry_certificates.txt
          echo "$near_expiry_certificates.txt" > expiring_certificates.txt

      - name: Send Notification to Slack Channel
        id: slack
        uses: slackapi/slack-github-action@v1.23.0
        with:
          # For posting a rich message using Block Kit
          payload: |
            {
              "text": "The certificate `cat near_expiry_certificates.txt` will expire in 400 days."
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          #SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK

      - name: Notify Slack
        uses: rtCamp/action-slack-notify@v2.1.0
        with:
          status: "danger"
          title: "Expiring Key Vault Certificates"
          text: "$(cat expiring_certificates.txt)"
          webhook_url: ${{ env.SLACK_WEBHOOK_URL }}














      # - name: Send Slack Notification
      #   uses: rtCamp/action-slack-notify@v3
      #   with:
      #     entryPoint: send
      #     args: |
      #       - channel=#my-slack-channel
      #       - webhook_url=${{ secrets.SLACK_WEBHOOK_URL }}
      #       - message="Hello, world!"
      #       - status=good

      #   #if: ${{ steps.cert-expiration.outputs.expiring_certs < 300 }}
      #   uses: rtCamp/action-slack-notify@master
      #   with:
      #     entryPoint: send
      #     args: |
      #       - channel=#pagerduty-alert-dump
      #       - webhook_url=${{ secrets.SLACK_WEBHOOK_URL }}
      #       - message="The certificate $near_expiry_certificates will expire in ${{ steps.cert-expiration.outputs.expiration }} days."
      #       - status=warning
      #   # with:
      #   #   status: "warning"
        #   message: "The certificate $near_expiry_certificates will expire in ${{ steps.cert-expiration.outputs.expiration }} days."
        #   channel: "#pagerduty-alert-dump"
        #   webhook_url: ${{ secrets.SLACK_WEBHOOK }}

      - name: Remove Runner IP to Key Vault Firewall
        run: az keyvault network-rule remove --name pdhstaging-keyvault --ip-address $runner_ip

      - name: Cleanup
        run: |
          unset runner_ip



      