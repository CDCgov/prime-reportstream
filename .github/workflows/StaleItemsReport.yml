name: Alert Stale Items from GitHub

on:
  workflow_dispatch:
  push: 
    branches:
      - feature/stale-items-report
  
jobs:
  alert_stale_items:
    name: Alert on Stale items in github
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Changes
        uses: actions/checkout@24cb9080177205b6e8c946b17badbe402adc938f
        with:
          fetch-depth: 0

      - name: Get Stale prs
        id: PullRequests
        shell: pwsh
        run: |
          ./.github/scripts/stale_items_report/StalePullRequests.ps1        

      - name: Get Stale prs
        id: PullRequestsShow
        shell: pwsh
        run: |
         echo  ${env:Stale_pullrequests} >> PullRequests.txt
        
      # - name: Get Stale branches
      #   id: PullRequestsprint
      #   shell: pwsh
      #   run: |
      #    cat file.txt

      - name: Get Stale branches
        id: Branches
        shell: pwsh
        run: |
          ./.github/scripts/stale_items_report/StaleBranches.ps1

      - name: Get Stale branches
        id: BranchesShow
        shell: pwsh
        run: |
         echo  ${env:Stale_Branches} >> Branches.txt

      - name: Get Stale Issues
        id: Issues
        shell: pwsh
        run: |
          ./.github/scripts/stale_items_report/StaleIssues.ps1

      - name: Get Stale branches
        id: IssuesShow
        shell: pwsh
        run: |
         echo  ${env:Stale_Issues} >> StaleIssues.txt

      # - name: Get Stale branches
      #   id: stalebranches
      #   run: |
      #     branches=$(./.github/scripts/stale_items_report/staleprs.sh)
      #      echo "StaleBranches<<EOF" >> $GITHUB_ENV
  
      #     # here we can place the command that will generate multi-line text
      #     echo $branches >> $GITHUB_ENV 
            
      #     echo "EOF" >> $GITHUB_ENV
      #   shell: bash

      # - name: print Stale branches
      #   id: printstalebranches
      #   run: |
      #     echo "${{ env.StaleBranches }}" >> oldbranches.txt
      #     cat oldbranches.txt
      #   shell: bash
      
      # - name: Get Stale prs
      #   id: PullRequests
      #   run: |
      #     prs=$(./.github/scripts/stale_items_report/staleprs.sh)
      #      echo "StalePRss<<EOF" >> $GITHUB_ENV
  
      #     # here we can place the command that will generate multi-line text
      #     echo $prs >> $GITHUB_ENV 
            
      #     echo "EOF" >> $GITHUB_ENV
      #   shell: bash
      - uses: MeilCli/slack-upload-file@v3
        id: message
        continue-on-error: true
        with:
          slack_token: ${{ secrets.SLACK_TOKEN }}
          channel_id: 'C04UBN6R9EF'
          file_path: 'PullRequests.txt'
          initial_comment: 'Stale pull requests report'
      # - run: 'echo ${{ fromJson(steps.message.outputs.response).files[0].file.permalink }}'

      - uses: MeilCli/slack-upload-file@v3
        id: Branchesmessage
        continue-on-error: true
        with:
          slack_token: ${{ secrets.SLACK_TOKEN }}
          channel_id: 'C04UBN6R9EF'
          file_path: 'Branches.txt'
          initial_comment: 'Stale branches report'

      - uses: MeilCli/slack-upload-file@v3
        id: Issuesmessage
        continue-on-error: true
        with:
          slack_token: ${{ secrets.SLACK_TOKEN }}
          channel_id: 'C04UBN6R9EF'
          file_path: 'StaleIssues.txt'
          initial_comment: 'Stale Issues report'

      # - run: 'echo ${{ fromJson(steps.message.outputs.response).files[0].file.permalink }}'

      # - name: Stale Items report
      #   id: slack
      #   uses: slackapi/slack-github-action@v1.23.0
      #   with:
      #     payload-file-path: "stale_prs.json"
      #   env:
      #     SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      # - name: Upload JSON file
      #   uses: actions/upload-artifact@v2
      #   with:
      #     name: data
      #     path: stale_prs.json

      # - name: Send Slack notification
      #   uses: rtCamp/action-slack-notify@v2
      #   with:
      #     status: ${{ job.status }}
      #     text: 'A new JSON file has been generated and attached.'
      #     title: 'JSON File Notification'
      #   env:
      #     SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
     