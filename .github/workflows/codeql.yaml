name: "Running CodeQL scanning on prime-reportstream repository"

on: push
  # on:
  # pull_request:
  #   branches:
  #     - master
  #   paths:
  #     - "prime-router/**"
jobs:
  Java_Codeql_Scan:
    name: Codeql_Scan
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [ 'java' ]
    defaults:
      run:
        working-directory: prime-router

    steps:
    - name: Checkout repository
      uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v2
      with:
        languages: ${{ matrix.language }}

    - name: Checkout repository
      uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b

    - name: Set up JDK 11
      uses: actions/setup-java@2c7a4878f5d120bd643426d54ae1209b29cc01a3
      with:
         java-version: '11'
         distribution: 'adopt'
         cache: 'gradle'

    - uses: gradle/wrapper-validation-action@e6e38bacfdf1a337459f332974bb2327a31aaf4b

    - name: Spin up build containers
      run: docker-compose -f docker-compose.postgres.yml up -d

    - name: Build Prime Router Package
      run: ./gradlew package -Pshowtests

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v2

    - name: Cleanup Gradle Cache
      run: |
        rm -f .gradle/caches/modules-2/modules-2.lock
        rm -f .gradle/caches/modules-2/gc.properties

  python_javascript_Codeql_Scan:
    name: Codeql_Scan
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript', 'python' ]
    defaults:
      run:
        working-directory: prime-router

    steps:
    - name: Checkout repository
      uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v2
      with:
        languages: ${{ matrix.language }}

    - name: Checkout repository
      uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v2
