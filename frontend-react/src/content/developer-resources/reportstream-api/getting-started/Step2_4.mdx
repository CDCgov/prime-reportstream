import site from "../../../site.json";

After completing the mapping file, export the column headers and filled rows as a CSV file. Send the generated CSV your onboarding engineer or <A href={site.orgs.RS.email}>reportstream@cdc.gov</A>. We will review your codes and provide next steps to continue your onboarding. 