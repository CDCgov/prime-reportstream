// use commands for convenience, robustness in heavily repeated user actions
// NOTE: they are NOT a replacement for UX that should be checked explicitly in test flows

import "./commands/api";
import "./commands/auth";
import "./commands/organizations";
