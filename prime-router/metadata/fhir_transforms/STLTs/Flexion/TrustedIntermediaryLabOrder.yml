# Just a placeholder to keep the object mapper happy for now; this will be the sender transform for Trusted Intermediary
elements:
  - name: placeholder-element
    resource: 'Bundle.entry.resource.ofType(Patient)'
    condition: false
    bundleProperty: '%resource.name.text'
    value: [ '"placeholder value"' ]