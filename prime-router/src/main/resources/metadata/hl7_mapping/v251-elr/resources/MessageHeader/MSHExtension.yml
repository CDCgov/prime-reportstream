extends: classpath:/metadata/hl7_mapping/resources/MessageHeader/MSHExtension.yml
elements:

  - name: message-control-id
    value:
      - '%resource.extension("MSH.10").value'
    hl7Spec: [ MSH-10 ]

  # Remaining fields are deprecated in NIST
  - name: sending-network-address-from-extension
    condition: false

  - name: msh-25-receiving-network-address-duplicates-receiving-application
    condition: false